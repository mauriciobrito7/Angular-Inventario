import { CalculatorEarnForPricePipe } from './calculator-earn-for-price.pipe';

describe('CalculatorEarnForPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatorEarnForPricePipe();
    expect(pipe).toBeTruthy();
  });
});
