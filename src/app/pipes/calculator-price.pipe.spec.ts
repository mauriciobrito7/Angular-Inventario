import { CalculatorPricePipe } from './calculator-price.pipe';

describe('CalculatorPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatorPricePipe();
    expect(pipe).toBeTruthy();
  });
});
