import { CalculatorEarnPipe } from './calculator-earn.pipe';

describe('CalculatorEarnPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatorEarnPipe();
    expect(pipe).toBeTruthy();
  });
});
